<nav class="navbar navbar-default navbar-static-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#grecqo_nav">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
        <%= link_to root_path, class: 'navbar-brand navbar-brand-centered' do %>
          Grecqo
        <% end %>
    </div>

    <div class="collapse navbar-collapse" id="grecqo_nav">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to players_path do %>
            <%= fa_icon 'users' %> Spieler
          <% end %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <%= link_to activities_path do %>
              <%= fa_icon 'bell-o' %>
              <% if PublicActivity::Activity.where(recipient: current_user).any? %>
                <span class="notifications_label">
                  <%= PublicActivity::Activity.where(recipient: current_user).count %>
                </span>
              <% end %>
            <% end %>
          </li>
          <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Sign in", new_user_session_path %></li>
        <% end %>           
      </ul>
    </div>
  </div>
</nav>